## Neighborhood Counts

To get started, install dependencies:

```bash
brew install redis sqlite3
gem install bundle
bundle
```

Bootstrap Rails and start server:

```bash
rake db:migrate
rails s
```
